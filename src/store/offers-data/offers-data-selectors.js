export const getOffers = (state) => state.OFFERS.offers;

export const getFilteredOffers = (state) => state.OFFERS.filteredOffers;

export const getUnsortedOffers = (state) => state.OFFERS.unsortedOffers;

export const getActiveElement = (state) => state.OFFERS.activeElement;

export const getActiveSortType = (state) => state.OFFERS.activeSortType;
